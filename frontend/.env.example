# Optional for separate production hosting; local /api is proxied by Vite.
VITE_GHOST_API_URL=
# Set true only for static-only hosting without an API (e.g. GitHub Pages).
VITE_GHOST_STATIC_MODE=false
# Never put model keys in this directory or in VITE_* variables.
